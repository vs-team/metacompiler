- type inference
  - first stage should create generic types
  - second stage should unify the variables
  - type checker
    - verify arieties
    - resolution of < and > for generic types
    - higher order terms
    - equality with == on primitive types
- GUI should save delayed compiler to not recompile rules unnecessarily
- samples
  - import directive for multiple files
- Scoping for keywords
- Fix Func and Data (also in samples)
- left of => no call to new; use a function instead
- Default (empty) values for keyword definitions
- Refactor "clause" to "premise"
- Runnable wrapper for primitive types to remove need for $ operator
- Textual debugger for terms
  
  *) 
     s z + s z

  *) s z + z => res
     ------------------
     s z + s z => s res

  *) --------------
     s z + z => s z
     ------------------
     s z + s z => s res

  *) --------------
     s z + z => s z
     --------------------
     s z + s z => s (s z)

  *) s z + s z => s (s z)
