- general cleanup
  - TypeConstantDescriptor.FromType is way too weak; should use defined type names and also import statements
- Higher:
  - kinded generic parameters
  - Fixed parenthesis in C# method call arguments. EXAMPLE vn := <<Vector3.Normalize(new Vector3(0.5,0.5,0.5))>>