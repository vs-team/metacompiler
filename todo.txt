- general cleanup
  - Application(Angle, Application(Angle, Extension({Var.Name = name},_,_) :: [], _, _) :: [], _, _) as a -> 
    IS NOT NECESSARILY NativeValue
  - TypeConstantDescriptor.FromType is way too weak
- type inference
  - Generic types
- ParsedKeyword cleanup
  - ReturnType
  - BaseType
  - remove typeToString
  - remove decodeName

- cache parsed rules and type inferenced results
- samples
  - import directive for multiple files
- Refactor "clause" to "premise"
- Keyword declarations between clauses
- Refactor CodeGenerator.fs
- Higher:
  - kinded generic parameters
  - order terms

- Textual debugger for terms
  
  *) 
     s z + s z

  *) s z + z => res
     ------------------
     s z + s z => s res

  *) --------------
     s z + z => s z
     ------------------
     s z + s z => s res

  *) --------------
     s z + z => s z
     --------------------
     s z + s z => s (s z)

  *) s z + s z => s (s z)


- 234 trees recursive contains
    -----------------------
    contains empty k => no

    containsIterate l k => res
    ---------------------------
    contains node l k => res

    contains r k => res
    -------------------------
    containsIterate r;nil k => res

    k = x
    --------------------------------
    containsIterate l;x;xs k => yes

    k < x
    contains l k => res
    --------------------------------
    containsIterate l;x;xs k => res
