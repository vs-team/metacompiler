- type inference
  - normalisation
  - test/adapt all samples
  - integrate into code generator
  - generic tuples, generic lists
- GUI should save delayed compiler to not recompile rules unnecessarily
- samples
  - import directive for multiple files
- Refactor "clause" to "premise"
- Runnable wrapper for primitive types to remove need for $ operator
- Scoping for keyword declarations
- Func and Data
  - ParsedKeyword should be a richer definition
  - Fix in all samples
- Higher order terms
- Higher kinded generics
- Type classes/modules
- Textual debugger for terms
  
  *) 
     s z + s z

  *) s z + z => res
     ------------------
     s z + s z => s res

  *) --------------
     s z + z => s z
     ------------------
     s z + s z => s res

  *) --------------
     s z + z => s z
     --------------------
     s z + s z => s (s z)

  *) s z + s z => s (s z)


- 234 trees recursive contains
    -----------------------
    contains empty k => no

    containsIterate l k => res
    ---------------------------
    contains node l k => res

    contains r k => res
    -------------------------
    containsIterate r;nil k => res

    k = x
    --------------------------------
    containsIterate l;x;xs k => yes

    k < x
    contains l k => res
    --------------------------------
    containsIterate l;x;xs k => res
